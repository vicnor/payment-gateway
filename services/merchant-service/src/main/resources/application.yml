spring:
  application:
    name: merchant-service
  jackson:
    property-naming-strategy: SNAKE_CASE
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

server:
  port: 8101

management:
  endpoints:
    web:
      exposure:
        include: health
